import { PerformanceData } from '../../typings/typings';
import React from 'react';

type Props = {
  performanceData: PerformanceData;
};

const GeneralInfoMonitor: React.FC<Props> = () => {
  return (
    <div>GeneralInfoMonitor</div>
  );
};

export default GeneralInfoMonitor;
